[project]
name = "truco"
version = "0.1.0"
description = "2-player Truco CFR agent"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
